
<MyLayout>
    BoxLayout:
        orientation: 'vertical'
        spacing: 0
        padding: 0

        Button:
            id: btn
            text: '-'
            on_release: playback.open(self)
            size_hint_y: None
            height: '48dp'

        Widget:
            on_parent: playback.dismiss()

        DropDown:
            id: playback
            on_select: btn.text = 'Selected value: ' + str(args)

            Button:
                text: 'Value A'
                size_hint_y: None
                height: '48dp'
                on_release: playback.select('A')

            Button:
                text: 'Value B'
                size_hint_y: None
                height: '48dp'
                on_release: playback.select('B')

            Button:
                text: 'Value C'
                size_hint_y: None
                height: '48dp'
                on_release: playback.select('C')